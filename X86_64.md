# Benchmark overview — x86_64

> Linux 6.17.0-1020-gcp, x86_64, pinned to cpu 1. Generated 2026-07-12 by
> `compare2upstream/run.sh` (`min_time=1s`,
> `repetitions=5`; cells are medians over config-alternating rounds).
> `fork` is nanofix; `fork-no-simd` is the same code with
> `NANOFIX_DISABLE_SIMD`; `upstream` is jamesdbrock/hffix.

Write/read latency — lower is better; first column is the baseline. Iterator path (`find_with_hint` over a fixed 20-tag set per message); tail percentiles include the latency probe (RDTSCP ~5-10 cycles on x86-64, steady_clock ~20-30 ns elsewhere):

| Benchmark | upstream | fork | fork-no-simd |
| --- | --- | --- | --- |
| `Write p99` | 236.8 ns | 132.9 ns (-44 %) | **125.8 ns (-47 %)** |
| `Write p999` | 248.4 ns | 143.9 ns (-42 %) | **136.8 ns (-45 %)** |
| `Read seq p99` | 80831 ns | **78251 ns (-3 %)** | 79416 ns (-2 %) |
| `Read seq p999` | 83838 ns | **81436 ns (-3 %)** | 82663 ns (-1 %) |
| `Read rand p99` | 117182 ns | 114541 ns (-2 %) | **113195 ns (-3 %)** |
| `Read rand p999` | 120901 ns | 118426 ns (-2 %) | **117113 ns (-3 %)** |

Write/read throughput — higher is better; iterator path, like-for-like, first column is the baseline:

| Benchmark | upstream | fork | fork-no-simd |
| --- | --- | --- | --- |
| `Write (NewOrder)` | 5.12 M msgs/s | 5.85 M msgs/s (+14 %) | **5.88 M msgs/s (+15 %)** |
| `Read seq` | 30.4 k msgs/s | 30 k msgs/s (-1 %) | **33.4 k msgs/s (+10 %)** |
| `Read random` | 22 k msgs/s | **22.3 k msgs/s (+1 %)** | 20.6 k msgs/s (-6 %) |

New access path vs the upstream iterator — latency (ns), lower is better. Same 20-tag workload as the throughput table below; reading many tags makes the iterator rescan per lookup, while `build_field_index` is O(length) regardless of lookup count. Shown SIMD-on (`fork`) and SIMD-off (`fork-no-simd`). Tail percentiles include the latency probe (RDTSCP ~5-10 cycles on x86-64, steady_clock ~20-30 ns elsewhere):

| Benchmark | upstream iterator | fork indexed | fork-no-simd indexed |
| --- | --- | --- | --- |
| `Read seq p99` | 80831 ns | **8121 ns (-90 %)** | 16799 ns (-79 %) |
| `Read seq p999` | 83838 ns | **11111 ns (-87 %)** | 19425 ns (-77 %) |
| `Read rand p99` | 117182 ns | **8421 ns (-93 %)** | 21452 ns (-82 %) |
| `Read rand p999` | 120901 ns | **11363 ns (-91 %)** | 24142 ns (-80 %) |

New access path vs the upstream iterator — message throughput, higher is better, delta vs upstream's iterator on the same workload. Shown SIMD-on (`fork`) and SIMD-off (`fork-no-simd`):

| Benchmark | upstream iterator | fork indexed | fork-no-simd indexed |
| --- | --- | --- | --- |
| `Read seq` | 30.4 k msgs/s | **311 k msgs/s (+924 %)** | 148 k msgs/s (+387 %) |
| `Read random` | 22 k msgs/s | **295 k msgs/s (+1243 %)** | 116 k msgs/s (+428 %) |

Index amortization — how many `find()`s per message before `build_field_index` + indexed lookups beat the iterator (`find_with_hint`), same binary, same messages (per-message ns). Break-even interpolated from `BM_Parse_FindN_{Iter,Indexed}`; below it, iterate — above it, index:

| Config | Break-even | iter @3 | indexed @3 | iter @30 | indexed @30 |
| --- | --- | --- | --- | --- | --- |
| `fork` | **~6.9 finds/msg** | 146.4 ns | 2852 ns | 56376 ns | 3388 ns |
| `fork-no-simd` | **~10.8 finds/msg** | 146.4 ns | 5110 ns | 56501 ns | 7507 ns |

